# 탑퀀트 대시보드 — 프로젝트 인수인계 노트

> **용도**: 새 Claude 대화(세션)에서 이 프로젝트를 이어갈 때 컨텍스트 복원용.
> 새 대화에서 이렇게 시작하세요: *"topquant-dashboards 저장소의 PROJECT_NOTES.md를 읽고 이어가자"*
> 최종 갱신: 2026-07-05 (PR #30까지 반영)

## 1. 프로젝트 개요

금융 시장 모니터링 + **매수/매도 시점 판단** 대시보드 모음. 단일 HTML 파일들로 구성, 빌드 없음.

- **라이브**: https://topbanker99-ai.github.io/topquant-dashboards/ (기본) · https://topquant-dashboards.vercel.app/ (미러, main 푸시 시 자동 배포)
- **저장소**: `topbanker99-ai/topquant-dashboards` (프론트) · `topbanker99-ai/fred-proxy` (Vercel 서버리스 프록시)

## 2. 작업 관례 (중요)

- **PR 방식**: 브랜치에 커밋 → PR 생성 → 사용자가 "운영반영" 하면 머지. **머지는 반드시 사용자 지시 후.**
- 작업 브랜치는 세션마다 지정됨. 직전 PR이 머지됐으면 `git checkout -B <branch> origin/main`으로 재시작.
- **검증 관례**: 커밋 전 헤드리스 Chromium(`/opt/pw-browsers/.../chrome`, playwright는 `/opt/node22/lib/node_modules/playwright`)으로 파싱 에러 0·기능 동작 확인 + 신호 로직은 실데이터로 수치 검증. 스크린샷 첨부.
- GitHub Pages는 가끔 배포 큐 장애가 있음(githubstatus.com 확인). Vercel 미러는 즉시 반영.
- 세션 컨테이너가 재시작되면 브랜치가 옛 스냅샷에 얹힐 수 있음 — **커밋 후 `git log`로 부모가 최신 main인지 반드시 확인.**

## 3. 데이터 계층 — fred-proxy (https://fred-proxy-nine.vercel.app)

| 엔드포인트 | 소스 | 용도 |
|---|---|---|
| `/api/fred?series_id=&limit=&sort_order=desc` | FRED | 금리·VIX·신용·NFCI·Sahm·버핏지표 등 |
| `/api/indices?symbols=&range=` | Yahoo (일봉 종가만, 거래량 없음) | 지수·환율·개별주 |
| `/api/kr?path=` | 한국은행 ECOS | 반도체 수출금액지수(403Y001/30911AA) 등 |
| `/api/krx` | 공공데이터포털 주식시세 | 삼성·하이닉스 시총·종가 |
| `/api/slb?op=getSlbStockRank&stdDt=` | 예탁결제원 증권대차 | 대차잔고(공매도 대기), XML 응답 |
| `/api/nps` | 국민연금 포트폴리오 공시 | (보드에선 자체 시뮬 사용) |
| `/api/quote` | KIS | 실시간 현재가 스냅샷 |
| `/api/gov` | TreasuryDirect·FiscalData | 국채 보드 |

키는 전부 Vercel 환경변수. 프론트에서 직접 호출.

## 4. 합의된 투자 전략 프레임 (모든 보드 설계의 전제)

- **대상: 지수** (KOSPI·NASDAQ, 실행은 지수 ETF). 개별주는 삼성전자·SK하이닉스까지만(대차·시총·수출 데이터가 종목 단위로 있는 유일한 예외, "반지수").
- **기간: 전술 1~2개월 / 전략 3~6개월** 스윙·포지션. 판단 주기는 주 1회. **초단타 금지** — 데이터가 일봉·주간·월간이라 못 받침(틱·호가·실시간 뉴스 없음).
- 운용 사이클: **매수 보드(진입) ↔ 매도 보드(경계/청산)**, 같은 데이터·같은 지평.

## 5. 보드 현황

| 파일 | 내용 |
|---|---|
| `index.html` | 허브 — 히어로(엠블럼·라이브 티커)+매매판단 3대형카드+모니터링 8카드 |
| `topquant_buy.html` | **매수 시점** — 🇰🇷KOSPI/🇺🇸NASDAQ 2탭. 구조 5신호(금리·금융환경·이익엔진·Sahm·버핏) × 타이밍 6신호(눌림목[200일선 위 −4~−20%만]·과매도반전[RSI+볼린저]·VIX공포통과·KR:원화+대차/연금 | US:10년물+HY스프레드·추세[+급락일 가드]). 판정 5단계(보류→신중→관망→관심→분할매수), 역발상 조합 승격, 분할플랜(0/−4/−8/−13%), 보라 배지 호버=근거 설명. 카드 22장 config 기반 JS 생성 |
| `topquant_sell.html` | **매도 시점** — 단기(≤1개월, 5신호)/중장기(3~6개월, 8신호) 2탭, 총 13신호. 결정적 조합: 단기=변동성+과열, 중장기=이익둔화+금리긴축 |
| `topquant_compare.html` | 글로벌 비교차트 — 지수7+환율+연준금리 칩 토글, 기간시작 대비 % 정규화(금리만 %p), **자국통화/달러/원화 기준 전환**(환산환율 KRW·JPY·TWD·CNY·EURUSD 역수), CVD 검증 9색 팔레트 |
| `topquant_fx.html` | 원/달러 방향 — 5동인 신호엔진 + 3·6·12개월 평균(63/126/252거래일) 표기 |
| 나머지 | macro(FRED 30지표)·treasury·news·realtime·heatmap·nps(연금 시뮬)·buffett |

공통: 표준 topnav(11링크+엠블럼)·파비콘(인라인 SVG)·OG 메타+`assets/og.png` 공유 이미지. 15분 자동 갱신.

## 6. 참고 자료 반영 이력

- 신영증권 박사 인터뷰 분석 → 매도 보드 원설계 (닷컴 고점 패턴).
- 과거 급락 7건 분석 → 매도 보드 단기/중장기 2탭 분리 근거.
- **매수전략 리서치 자료집(유튜브 30편 정리 docx)** → 매수 보드에 4규칙 이식: ①눌림목은 상방추세에서만 ②볼린저 하단 복귀 ③N분할 매수 플랜 ④급락일(−3%) 진입 회피. 제외: 호가창·거래대금·테마·시초가(실시간 데이터 없음+개별주 단타), 거래량(프록시가 종가만 반환).

## 7. 미결 항목 (다음 작업 후보)

1. **힉스필드 이미지 에셋** — 커넥터가 세션에서 순단 반복으로 미완. 새 대화에서 연결 확인 후: 히어로 배경 아트, 로고 변형 시안 등. (파비콘·엠블럼·OG는 SVG/HTML렌더로 이미 완성)
2. **v2 데이터 확보 시 보강** (사용자가 자료 제공 예정):
   - 코스피 **외국인 순매매 일별** → 매수/매도 단기 신호 강화 (KRX는 anti-bot으로 직접수집 불가 확인됨)
   - **경제 이벤트 캘린더**(FOMC·BOJ·CPI·옵션만기) → "이벤트 D-day" 플래그
   - 개인 **신용융자잔고** → ECOS에 없음, KOFIA/KRX 모두 실패 이력 있음
   - **거래량** → fred-proxy indices.js가 종가만 반환, 확장 필요
3. 매도 보드 v2 후보: VIX 급등·신용스프레드 급확대 독립 신호화.

## 8. 알려진 제약

- Yahoo 프록시는 15~20분 지연 일봉. KRX 정보데이터시스템은 anti-bot(`LOGOUT`)으로 사용 불가.
- 국민연금 신호는 2026-03말 공시 기준 자기보정 시뮬(실제 공시와 오차 가능).
- GitHub MCP 커넥터 토큰이 가끔 만료 — git push 인증(로컬 프록시)은 별개로 동작하므로 머지가 급하면 main 직접 병합 가능(사용자 명시 지시 시에만).
